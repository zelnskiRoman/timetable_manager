import { Component, Input } from '@angular/core';import { IGroup, IUnivDirection } from '../add-edit.interfaces';@Component({  selector: 'app-univ-item',  templateUrl: './university-item.component.html',  styleUrls: ['./university-item.component.sass']})export class UniversityItemComponent {  @Input() directionData: IUnivDirection;  @Input() isSelected: boolean;  @Input() editPanelStyle: 'tutor' | 'group';  showAddPanel: boolean;  constructor() {    this.isSelected = false;  }  getTitle(): string {    return this.directionData.title;  }  getSubItems(): IGroup[] {    return this.directionData.groups;  }  onAddClick(): void {    this.showAddPanel = true;  }  addTutorToLesson(_) {    return;  }}